image: node:18

pipelines:
  default:
    - step:
        name: Build and Test
        caches:
          - node
        script:
          - echo "Installing dependencies..."
          - npm install
          - echo "Running tests..."
          - npm test
